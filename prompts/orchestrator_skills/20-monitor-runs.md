requires: view_runs
modes: orchestrator

# Контроль выполнения

- Используй `view_runs`, `get_world_state` и `wait_for_event`, чтобы не терять контекст задач.
- Если run завис, меняй стратегию или заменяй задачу, а не повторяй одно и то же.
- Для каждой проверки фиксируй, что именно было подтверждено.
